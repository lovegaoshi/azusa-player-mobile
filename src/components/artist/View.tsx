import { ScrollView, Text } from 'react-native';

import FlexView from '@components/commonui/FlexViewNewArch';

export default () => {
  return (
    <FlexView>
      <ScrollView>
        <Text>wow really good</Text>
      </ScrollView>
    </FlexView>
  );
};
